import { StyleSheet } from 'react-native';

const styles = {

  imageContainer: {
    flexDirection: 'row',
    marginTop: 20,
    marginBottom: 20,
  },

  image: {
    flex: 1,
    resizeMode: 'contain',
  }

};

export default StyleSheet.create(styles);

