import { StyleSheet } from 'react-native';

const styles = {

  imageContainer: {
    flexDirection: 'row',
    marginTop: 20,
    marginBottom: 20,
  },

};

export default StyleSheet.create(styles);

