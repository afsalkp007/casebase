/* eslint-disable max-len */
import React from 'react';
import {
  View,
} from 'react-native';
import BodyText from '../../../../BodyText';
import ItalicText from '../../../../ItalicText';

const Body = function Body() {
  return (
    <View>
      <BodyText>
        Now, to complement that estimated fair price, conduct a valuation using multiples. You could calculate worth using a <ItalicText>revenue</ItalicText> multiple, the most common approach recently in an M&A market dominated by the sales of several web-based content production businesses.
      </BodyText>
      <BodyText>
        Alternatively, you could use the approach more common in earlier years, when content production companies or divisions were sold on a <ItalicText>profit</ItalicText> multiple basis. Which approach do you think would provide a more accurate valuation of Marlin’s group? Why?
      </BodyText>
    </View>
  );
};

export default Body;
/* eslint-enable max-len */
