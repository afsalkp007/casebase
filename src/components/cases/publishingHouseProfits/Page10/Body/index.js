/* eslint-disable max-len */
import React from 'react';
import {
  View,
} from 'react-native';
import BodyText from '../../../../BodyText';

const Body = function Body() {
  return (
    <View>
      <BodyText>
        Do you think this is a solid estimate for the price Marlin should seek for its group? Explain why.
      </BodyText>
      <BodyText>
        In wrapping up, present a three sentence recommendation for the head of Marlin.
      </BodyText>
    </View>
  );
};

export default Body;
/* eslint-enable max-len */
