import { StyleSheet } from 'react-native';

const styles = {

  textInput: {
    textAlign: 'right',
    paddingRight: 10,
    borderWidth: 0,
    fontSize: 20,
    minHeight: 40,
  },

};

export default StyleSheet.create(styles);

