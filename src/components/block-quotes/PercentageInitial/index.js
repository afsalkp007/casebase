import React from 'react';
import BlockQuote from '../../BlockQuote';
import I from '../../ItalicText';

const PercentageInitial = () => (
  <BlockQuote>
    Report your answer in percentage <I>points</I> to the nearest tenth.
    You may omit the decimal value if it is zero, and answers need not be exact.
    For example, 22.2% could be represented as 22 (but <I>not</I> .22).
  </BlockQuote>
);

export default PercentageInitial;
